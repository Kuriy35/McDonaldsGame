<UserControl x:Class="McDonalds.Views.EndDayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:McDonalds.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid Background="#F5F5F5">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="День завершено!" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="{Binding Message}" FontSize="20" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Money, StringFormat='Зароблено: ${0:F2}'}" FontSize="16" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding Satisfaction, StringFormat='Задоволеність: {0:P0}'}" FontSize="16" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Рестарт" Command="{Binding RestartCommand}" Style="{StaticResource GreenButtonStyle}"/>
                <Button Content="Вихід" Command="{Binding ExitCommand}" Style="{StaticResource RedButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>